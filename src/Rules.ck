/**
*  Rules interface. 
*  Currently will need to be overridden, so only interface
*  provided. 
*
*  @author: Iain Emsley
*/


public class IRule {
   fun int intrules (string str) {}
   fun float fltrules(string str) {}
   fun string strrules(string str) {}
   // float
   fun float fltToStr(float i) {}
}
