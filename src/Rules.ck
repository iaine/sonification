/**
*  Rules interface. 
*  Currently will need to be overridden, so only interface
*  provided. 
*
*  @author: Iain Emsley
*/


class IRule {
   fun int rules () {}
   fun float rules() {}
   fun string rules() {}
}

public class Rules extends IRule {
   fun int rules () {}
   fun float rules() {}
   fun string rules() {}
}
